{
  "$schema": "../gen/schemas/desktop-schema.json",
  "description": "Capability for the main window",
  "identifier": "default",
  "permissions": [
    "core:default",
    "opener:default",
    "fs:allow-read",
    "fs:allow-write"
  ],
  "windows": ["main"]
}
